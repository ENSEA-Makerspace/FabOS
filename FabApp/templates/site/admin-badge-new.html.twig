<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Créer un badge - FabOS</title>
    <link rel="stylesheet" href="{{ asset('css/style.css') ~ '?v=20260731-tokens' }}">
    <link rel="stylesheet" href="{{ asset('css/admin.css') ~ '?v=20260731-tokens' }}">
    <link rel="stylesheet" href="{{ asset('css/details.css') ~ '?v=20260801-s48' }}">
    <link rel="icon" type="image/png" href="{{ asset('images/favicon.png') }}">
    <style>
        @media (max-width: 900px) { .admin-edit-layout { grid-template-columns: 1fr; padding: 0 16px; } .form-grid { grid-template-columns: 1fr; } .admin-edit-header { padding: 24px 16px; } }
    </style>
</head>
<body>
    {% include 'site/_header.html.twig' %}
    <main>
        <header class="admin-edit-header">
            <div class="admin-edit-header-inner">
                <div>
                    <h1>Créer un badge</h1>
                    <p>Création d’un badge seul, sans rattachement formation, machine ou utilisateur.</p>
                </div>
                <a class="btn-link" href="{{ path('app_admin_badges') }}">Retour aux badges</a>
            </div>
        </header>

        <div class="admin-edit-layout">
            {% include 'site/_admin_sidebar.html.twig' with {admin_sidebar_variant: 'edit'} %}

            <section class="admin-edit-panel">
                <div class="admin-edit-panel-header">
                    <h2>Nouveau badge</h2>
                    <p>Le badge sera disponible ensuite dans les formulaires formations et machines.</p>
                </div>

                {{ form_start(form, {attr: {class: 'admin-edit-form', novalidate: 'novalidate'}}) }}
                    {{ form_errors(form) }}
                    <div class="form-grid">
                        <div class="form-field">
                            {{ form_label(form.nom) }}
                            {{ form_widget(form.nom) }}
                            <div class="form-errors">{{ form_errors(form.nom) }}</div>
                        </div>
                        <div class="form-field">
                            {{ form_label(form.icone) }}
                            {{ form_widget(form.icone) }}
                            <div class="form-errors">{{ form_errors(form.icone) }}</div>
                        </div>
                        <div class="form-field full">
                            {{ form_label(form.description) }}
                            {{ form_widget(form.description) }}
                            <div class="form-errors">{{ form_errors(form.description) }}</div>
                        </div>
                        <div class="form-field full">
                            {{ form_label(form.institutions) }}
                            {{ form_widget(form.institutions) }}
                            <div class="form-errors">{{ form_errors(form.institutions) }}</div>
                        </div>
                        <div class="form-field">
                            {{ form_label(form.prerequisiteBadge) }}
                            {{ form_widget(form.prerequisiteBadge) }}
                            <div class="form-errors">{{ form_errors(form.prerequisiteBadge) }}</div>
                        </div>
                    </div>

                    <div class="form-actions">
                        <a class="btn-link" href="{{ path('app_admin_badges') }}">Annuler</a>
                        <span class="btn-submit">{{ form_widget(form.save, {label: 'Créer le badge'}) }}</span>
                    </div>
                {{ form_end(form) }}
            </section>
        </div>
    </main>
</body>
</html>
