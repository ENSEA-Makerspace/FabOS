{% extends 'base.html.twig' %}

{# S78 step 7 — this page owned its <!DOCTYPE> and <head>. With no layout
   there was nowhere to put a stylesheet link, which is why every one of
   these grew a <style> and every <style> grew literal colours. The shell
   emits the doctype, style.css, the favicon and importmap('app').

   ⚠️ It GAINS Stimulus — the shell emits `importmap('app')` and this page
   did not, so `confirm_controller` and friends now actually run here. A
   delete button wired to a controller that never loaded deletes without
   asking, so that is the fix rather than a side effect. #}

{% block title %}{{ 'materials.new_browser_title'|trans }}{% endblock %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/admin.css') ~ '?v=20260816-s139e' }}">
    <link rel="stylesheet" href="{{ asset('css/details.css') ~ '?v=20260816-s139e' }}">
    <style>
        .form-field textarea { min-height: 110px; resize: vertical; }
        .material-machines-choices { display: grid; grid-template-columns: repeat(auto-fill, minmax(170px, 1fr)); gap: 6px 14px; }
        .material-machines-choices input { width: auto; margin-right: 7px; }
        .material-machines-choices label { font-weight: 400; display: inline-flex; align-items: center; }
        @media (max-width: 900px) {.admin-edit-header { padding: 24px 16px; }}
    </style>
{% endblock %}

{% block body %}
    {% include 'site/_header.html.twig' %}
    <main>
        <header class="admin-edit-header">
            <div class="admin-edit-header-inner">
                <div>
                    <h1>{{ 'materials.new_title'|trans }}</h1>
                    <p>{{ 'materials.new_subtitle'|trans }}</p>
                </div>
                <a class="btn-link" href="{{ path('app_admin_materials') }}">{{ 'materials.back'|trans }}</a>
            </div>
        </header>

        <div class="admin-edit-layout">
            {% include 'site/_admin_sidebar.html.twig' with {admin_sidebar_variant: 'edit'} %}

            <section class="admin-edit-panel">
                <div class="admin-edit-panel-header"><h2>{{ 'materials.panel_new'|trans }}</h2></div>
                {{ include('site/_material_form.html.twig', {form: form, submit_label: 'materials_form.submit_new'|trans}) }}
            </section>
        </div>
    </main>
{% endblock %}
