<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Calendrier - FabOS</title>
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
    <link rel="stylesheet" href="{{ asset('css/calendar-leaderboard.css') }}">
    <link rel="stylesheet" href="{{ asset('css/calendar-fix.css') }}">
    <link rel="icon" type="image/png" href="{{ asset('images/favicon.png') }}">
</head>
<body>
    <div class="alert-bar">
        {% include 'site/_alert_bar.html.twig' %}
    </div>

    <header class="site-header">
        <div class="header-container">
            <a href="{{ path('app_home') }}" class="header-brand">
                <div class="logo-ensea">
                    <svg width="127" height="125" viewBox="0 0 127 125" fill="none" xmlns="http://www.w3.org/2000/svg" class="ensea-logo-svg" aria-label="Logo ENSEA">
<path d="M78.4322 89.4308C79.1325 89.2813 79.8279 89.0999 80.532 88.9945C84.0423 88.47 87.5724 88.0889 91.0507 87.421C95.403 86.454 99.6371 85.0225 103.679 83.1514C107.244 81.7385 110.694 80.0546 113.998 78.1147C116 76.861 117.935 75.5081 119.899 74.1932C119.989 74.1319 120.034 74.0106 120.001 73.7851C119.645 73.9677 119.294 74.1527 118.938.7909C76.904 47.9206 76.5342 47.9747 76.1657 47.9495C75.8824 47.9687 75.6052 48.0398 75.348 48.159C74.7115 48.5417 74.0388 48.8619 73.3396 49.1149C70.542 49.7779 67.7159 50.3404 64.9059 50.9703C64.613 51.031 64.3489 51.1866 64.1549 51.4127C63.2421 52.6884 62.3911 54.0033 61.4845 55.284C59.3479 58.4054 58.2721 62.1224 58.4139 65.893C58.4199 67.6046 58.7676 69.298 59.4368 70.8755C60.1061 72.4531 61.0838 73.8835 62.3133 75.0841C62.9581 75.7195 63.7065 76.2423 64.5267 76.6306C67.1603 78.0121 70.0155 78.9297 72.9654 79.3426C75.7741 79.6845 78.5335 80.4578 81.4102 80.2152C82.5391 80.1208 83.6681 80.0926 84.8032 80.0326C84.9286 80.0392 85.0529 80.0598 85.1737 80.0939C81.8236 81.0141 78.3172 81.2323 74.8774 80.7348C75.0672 80.869 75.2829 80.9629 75.511 81.0105C76.5587 81.1868 77.6191 81.279 78.6817 81.2863C82.2303 81.1037 85.7852 80.9492 89.3041 80.535C91.5831 80.2047 93.8341 79.7062 96.0383 79.0436C97.8293 78.5534 99.5708 77.8953 101.317 77.2593C103.497 76.464 105.679 75.6527 107.832 74.7851C109.823 73.9848 111.784 73.1 113.751 72.2446C114.095 72.0951 114.524 71.714 114.763 71.8133C115.057 71.9076 115.369 71.9322 115.675 71.8851C115.98 71.8381 116.27 71.7207 116.521 71.5424C119.82 69.9681 123.111 68.3995 126.395 66.8366V0H125.538ZM39.349 111.08V124.954H48.0273C45.1284 120.326 42.2863 115.775 39.349 111.08ZM119.563 121.431C119.536 121.262 119.443 121.111 119.304 121.01C119.166 120.908 118.993 120.865 118.822 120.89C116.669 120.912 114.511 120.917 112.359 120.89C112.268 120.877 112.176 120.882 112.087 120.904C111.998 120.926 111.915 120.966 111.841 121.02C111.768 121.074 111.706 121.142 111.659 121.22C111.612 121.298 111.581 121.385 111.568 121.475C111.252 122.455 110.895 123.436 110.556 124.416C110.504 124.598 110.463 124.782 110.433 124.969H120.785C120.367 123.759 119.945 122.605 119.572 121.439L119.563 121.431ZM118.016 116.951C117.204 114.61 116.419 112.335 115.575 109.9C114.729 112.33 113.944 114.599 113.127 116.951H118.016ZM108.628 76.9395C108.796 76.9046 108.961 76.8583 109.122 76.801C111.293 75.8672 113.473 74.9628 115.621 73.9824C118.902 72.4799 122.14 70.8844 125.445 69.4211C125.627 69.3749 125.798 69.2925 125.948 69.1789C126.097 69.0654 126.221 68.923 126.314 68.7604C126.406 68.5978 126.464 68.4183 126.485 68.2328C126.505 68.0473 126.488 67.8596 126.433 67.6809C126.433 67.6479 126.389 67.6197 126.338 67.5584C126.202 67.6124 126.068 67.6733 125.938 67.741C124.469 68.6246 122.917 69.3255 121.46 70.2704C118.807 71.8648 116.071 73.3206 113.264 74.6319C111.734 75.4101 110.154 76.1025 108.59 76.8304C108.602 76.8635 108.618 76.8966 108.63 76.9297L108.628 76.9395ZM52.4159 79.888C51.5809 77.3635 50.7966 74.9946 50.0122 72.6307C49.9582 75.2506 50.806 77.8105 52.4159 79.888ZM55.3247 84.1772L53.2348 81.9848C53.157 82.0571 53.0792 82.1282 53.0014 82.2054C53.7029 82.9235 54.4082 83.6416 55.1098 84.3536L55.3247 84.1772ZM78.4322 89.4369C76.8742 89.4753 75.3167 89.5162 73.7596 89.5595C73.7596 89.6428 73.7596 89.72 73.7707 89.8046C75.3221 89.682 76.8808 89.5595 78.4322 89.4455V89.4369ZM118.946 74.3488C118.295 74.6196 117.65 74.8954 116.998 75.1711L117.115 75.4027L118.935 74.3427C118.935 74.3427 118.946 74.3537 118.935 74.3537L118.946 74.3488Z" fill="currentColor"/>
</svg>
                </div>
            </a>

            <nav class="main-navbar">
                <div class="navbar-container">
                    <a href="{{ path('app_home') }}" class="navbar-link">Accueil</a>
                    <a href="{{ path('app_calendar') }}" class="navbar-link active">Calendrier</a>
                    <a href="{{ path('app_machines') }}" class="navbar-link">Machines</a>
                    <a href="{{ path('app_formations') }}" class="navbar-link">Formations</a>
                    <a href="{{ path('app_leaderboard') }}" class="navbar-link">Leaderboard</a>
                    {% if is_granted('ROLE_ADMIN') %}
                        <a href="{{ path('app_admin_dashboard') }}" class="navbar-link">Admin</a>
                    {% endif %}
                </div>
            </nav>

            <div class="header-right">
                <div class="header-search">
                    <input type="text" placeholder="Rechercher..." class="search-input-header">
                    <button class="search-button-header">
                        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <circle cx="11" cy="11" r="8"/>
                            <path d="m21 21-4.35-4.35"/>
                        </svg>
                    </button>
                </div>
                
                <div class="header-auth">
                    {% include 'site/_header_auth.html.twig' %}
                </div>

                <button class="mobile-nav-toggle" aria-label="Menu">
                    <span></span>
                    <span></span>
                    <span></span>
                </button>
            </div>
        </div>
    </header>

    <main class="main-content">
        <section class="calendar-section">
            <div class="container">

                <!-- En-tête du calendrier -->
                <div class="calendar-header">
                    <div class="calendar-title">
                        <h1 class="section-badge">Calendrier Hebdomadaire</h1>
                        <p class="calendar-subtitle">
                            Consultez les disponibilités des machines et réservez vos créneaux.
                        </p>
                    </div>

                    <!-- Navigation semaine -->
                    <div class="calendar-navigation">
                        <button class="btn btn-secondary" id="prev-week">
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16">
                                <polyline points="15,18 9,12 15,6"/>
                            </svg>
                            Semaine précédente
                        </button>
                        <div class="current-week-display" id="current-week-display">
                            Semaine 26 (22 - 28 Juin 2026)
                        </div>
                        <button class="btn btn-secondary" id="next-week">
                            Semaine suivante
                            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" width="16" height="16">
                                <polyline points="9,18 15,12 9,6"/>
                            </svg>
                        </button>
                    </div>
                </div>

                <!-- Contenu principal : sidebar + calendrier -->
                <div class="calendar-main-content">

                    <!-- Sidebar de filtres par catégorie -->
                    <aside class="calendar-filter-sidebar">
                        <h3 class="sidebar-title">Machines</h3>

                        <div class="filter-category">
                            <h4>Machines disponibles</h4>
                            {% for machine in machines %}
                                <div class="filter-checkbox-item">
                                    <input type="checkbox" id="machine-{{ machine.id }}" class="category-checkbox" data-category="machines" checked>
                                    <label for="machine-{{ machine.id }}">{{ machine.nom }} <span class="machine-status-indicator" title="{{ machine.statut }}">{{ machine.statut }}</span></label>
                                </div>
                            {% else %}
                                <p>Aucune machine enregistrée.</p>
                            {% endfor %}
                        </div>

                        <div class="filter-actions">
                            <button class="btn btn-secondary btn-sm btn-full-width" id="reset-filters">
                                Réinitialiser les filtres
                            </button>
                        </div>
                    </aside>

                    <!-- Zone calendrier -->
                    <div class="calendar-content-wrapper">

                        <!-- Légende -->
                        <div class="calendar-legend">
                            <div class="legend-item">
                                <span class="legend-color reservation"></span>
                                <span>Réservation (autre utilisateur)</span>
                            </div>
                            <div class="legend-item">
                                <span class="legend-color my-reservation"></span>
                                <span>Ma réservation</span>
                            </div>
                            <div class="legend-item">
                                <span class="legend-color maintenance"></span>
                                <span>En maintenance</span>
                            </div>
                            <div class="legend-item">
                                <span class="legend-color panne"></span>
                                <span>Machine en panne</span>
                            </div>
                            <div class="legend-item">
                                <span class="legend-color closed"></span>
                                <span>FabLab fermé</span>
                            </div>
                            <div class="legend-item">
                                <span class="legend-color free"></span>
                                <span>Disponible</span>
                            </div>
                        </div>

                        <div class="admin-panel reservation-create-panel" style="margin-top: 16px; padding: 16px;">
                            <h3 style="margin: 0 0 12px;">Réserver une machine</h3>
                            {% if app.user %}
                                <form id="reservation-form" class="reservation-form" style="display: grid; grid-template-columns: repeat(2, minmax(180px, 1fr)); gap: 12px; align-items: end;">
                                    <div class="form-group">
                                        <label for="reservation-machine">Machine</label>
                                        <select id="reservation-machine" name="machineId" class="filter-input" required>
                                            <option value="">Choisir une machine</option>
                                            {% for machine in machines %}
                                                <option value="{{ machine.id }}">{{ machine.nom }}</option>
                                            {% endfor %}
                                        </select>
                                    </div>
                                    <div class="form-group">
                                        <label for="reservation-date-debut">Date début</label>
                                        <input type="datetime-local" id="reservation-date-debut" name="dateDebut" class="filter-input" required>
                                    </div>
                                    <div class="form-group">
                                        <label for="reservation-date-fin">Date fin</label>
                                        <input type="datetime-local" id="reservation-date-fin" name="dateFin" class="filter-input" required>
                                    </div>
                                    <div class="form-group">
                                        <label for="reservation-motif">Motif</label>
                                        <input type="text" id="reservation-motif" name="motif" class="filter-input" maxlength="500" placeholder="Optionnel">
                                    </div>
                                    <div style="grid-column: 1 / -1; display: flex; gap: 12px; align-items: center; flex-wrap: wrap;">
                                        <button type="submit" class="btn btn-primary" id="reservation-submit">Réserver</button>
                                        <span id="reservation-message" aria-live="polite"></span>
                                    </div>
                                </form>
                            {% else %}
                                <p style="margin: 0;">Connectez-vous pour créer une réservation.</p>
                            {% endif %}
                        </div>

                        {% if reservations is empty %}
                            <div class="admin-panel" style="margin-top: 16px; padding: 16px;">Aucune réservation pour le moment.</div>
                        {% endif %}

                        <!-- Calendrier principal -->
                        <div id="weekly-calendar-container">
                            <div class="weekly-calendar">

                                <!-- En-tête des jours -->
                                <div class="calendar-days-header" id="days-header">
                                    <div class="calendar-time-header">Heure</div>
                                    <div class="calendar-day-header">Lun 22</div>
                                    <div class="calendar-day-header">Mar 23</div>
                                    <div class="calendar-day-header">Mer 24</div>
                                    <div class="calendar-day-header">Jeu 25</div>
                                    <div class="calendar-day-header">Ven 26</div>
                                    <div class="calendar-day-header">Sam 27</div>
                                    <div class="calendar-day-header">Dim 28</div>
                                </div>

                                <!-- Grille des créneaux (générée par JS) -->
                                <div class="calendar-slots" id="calendar-slots"></div>

                            </div>
                        </div>

                        <!-- Bandeau d'information sur la sélection -->
                        <div class="calendar-selection-info" id="calendar-selection-info">
                            <p>Sélection : <span id="selection-info"></span></p>
                            <button class="btn btn-primary btn-sm" id="reserve-btn" disabled>Création bientôt disponible</button>
                        </div>

                    </div><!-- /.calendar-content-wrapper -->
                </div><!-- /.calendar-main-content -->

            </div><!-- /.container -->
        </section>
    </main>

    <footer class="site-footer">
        <div class="footer-container">
            <div class="footer-section">
                <div class="footer-logo">
                    <span class="logo-text">FabOS</span>
                    <p>Système de gestion de FabLab</p>
                </div>
            </div>
            <div class="footer-section">
                <h4>Navigation</h4>
                <a href="{{ path('app_home') }}">Accueil</a>
                <a href="{{ path('app_machines') }}">Machines</a>
                <a href="{{ path('app_calendar') }}">Calendrier</a>
                <a href="{{ path('app_formations') }}">Formations</a>
                <a href="{{ path('app_leaderboard') }}">Leaderboard</a>
            </div>
            <div class="footer-section">
                <h4>Ressources</h4>
                <a href="#">Documentation</a>
                <a href="#">Support</a>
                <a href="#">API</a>
                <a href="#">Statut</a>
            </div>
            <div class="footer-section">
                <h4>Légal</h4>
                <a href="#">Mentions légales</a>
                <a href="#">Politique de confidentialité</a>
                <a href="#">Conditions d'utilisation</a>
            </div>
        </div>
        <div class="footer-bottom">
            <p>&copy; 2026 FabOS - Développé pour l'ENSEA. Tous droits réservés.</p>
            <p class="footer-version">Version 1.0.0</p>
        </div>
    </footer>

    <script src="{{ asset('js/main.js') }}"></script>
    <script>
        // ===================================================================
        // Calendrier Hebdomadaire — FabOS
        // ===================================================================

        const CALENDAR_CONFIG = {
            startHour: 8,
            endHour: 22,
            slotHeight: 40,
            minSlotDuration: 60  // une ligne = 1 heure
        };

        const MACHINES = {
{% for machine in machines %}
            {{ machine.id }}: { id: {{ machine.id }}, nom: {{ machine.nom|json_encode|raw }}, couleur: "#9E1B56", category: "machines" }{{ not loop.last ? ',' : '' }}
{% endfor %}
        };

        const RESERVATIONS = [
{% for reservation in reservations %}
            { machine_id: {{ reservation.machine.id }}, date: {{ reservation.dateDebut|date('Y-m-d')|json_encode|raw }}, day: {{ reservation.dateDebut|date('N') - 1 }}, start: {{ reservation.dateDebut|date('H:i')|json_encode|raw }}, end: {{ reservation.dateFin|date('H:i')|json_encode|raw }}, user: {{ reservation.utilisateur ? reservation.utilisateur.displayName|json_encode|raw : 'Utilisateur'|json_encode|raw }}, user_id: {{ reservation.utilisateur ? reservation.utilisateur.id : 'null' }} }{{ not loop.last ? ',' : '' }}
{% endfor %}
        ];

        // Horaires d'ouverture du FabLab
        // [heure_ouverture, heure_fermeture] ou null si fermé toute la journée
        const FABLAB_HOURS = {
            0: [8, 20],  // Lundi
            1: [8, 20],  // Mardi
            2: [8, 20],  // Mercredi
            3: [8, 20],  // Jeudi
            4: [8, 18],  // Vendredi
            5: null,     // Samedi (fermé)
            6: null      // Dimanche (fermé)
        };

        // Machines avec statut spécial (maintenance ou panne)
        const MACHINE_STATUS = {};

        // État de l'application
        let currentDate = new Date();
        let selectedCategories = { 'machines': true };
        let selectedSlot = null;
        const userId = {{ app.user ? app.user.id : 'null' }};
        const IS_AUTHENTICATED = {{ app.user ? 'true' : 'false' }};

        // ---------------------------------------------------------------
        // Initialisation
        // ---------------------------------------------------------------
        document.addEventListener('DOMContentLoaded', initCalendar);

        function initCalendar() {
            document.getElementById('prev-week')?.addEventListener('click', () => {
                currentDate.setDate(currentDate.getDate() - 7);
                renderCalendar();
            });

            document.getElementById('next-week')?.addEventListener('click', () => {
                currentDate.setDate(currentDate.getDate() + 7);
                renderCalendar();
            });

            document.querySelectorAll('.category-checkbox').forEach(checkbox => {
                checkbox.addEventListener('change', function () {
                    selectedCategories[this.dataset.category] = this.checked;
                    renderCalendar();
                });
            });

            document.getElementById('reset-filters')?.addEventListener('click', () => {
                document.querySelectorAll('.category-checkbox').forEach(cb => cb.checked = true);
                Object.keys(selectedCategories).forEach(k => selectedCategories[k] = true);
                renderCalendar();
            });

            // Délégation d'événement : la grille est régénérée à chaque
            // rendu (renderGrid), un seul écouteur permanent sur le
            // conteneur suffit donc, plutôt que d'en attacher un par cellule.
            document.getElementById('calendar-slots')?.addEventListener('click', handleCalendarClick);
            initReservationForm();

            renderCalendar();
        }

        // ---------------------------------------------------------------
        // Rendu principal
        // ---------------------------------------------------------------
        function renderCalendar() {
            renderHeader();
            renderGrid();
        }

        // ---------------------------------------------------------------
        // En-tête : numéro de semaine + labels des jours
        // ---------------------------------------------------------------
        function renderHeader() {
            const weekStart = getWeekStartDate(currentDate);
            const weekEnd   = new Date(weekStart);
            weekEnd.setDate(weekEnd.getDate() + 6);

            // Affichage "Semaine XX (DD - DD Mois YYYY)"
            const weekDisplay = document.getElementById('current-week-display');
            if (weekDisplay) {
                const opts = { day: 'numeric', month: 'long' };
                const startStr = weekStart.toLocaleDateString('fr-FR', opts);
                const endStr   = weekEnd.toLocaleDateString('fr-FR', { day: 'numeric', month: 'long', year: 'numeric' });
                weekDisplay.textContent =
                    `Semaine ${getWeekNumber(currentDate)} (${startStr} - ${endStr})`;
            }

            // Labels des colonnes jours
            const daysHeader = document.getElementById('days-header');
            if (daysHeader) {
                const days = getWeekDays();
                let html = '<div class="calendar-time-header">Heure</div>';
                days.forEach(day => {
                    const label   = day.toLocaleDateString('fr-FR', { weekday: 'short' });
                    const num     = day.getDate();
                    const isToday = isSameDay(day, new Date());
                    html += `<div class="calendar-day-header${isToday ? ' today' : ''}">${label} ${num}</div>`;
                });
                daysHeader.innerHTML = html;
            }
        }

        // ---------------------------------------------------------------
        // Grille : une ligne par heure, une colonne par jour
        //
        // CORRECTIF MAJEUR : la version précédente plaçait les cellules
        // dans le flux normal de la grille (auto-placement) puis masquait
        // les cellules couvertes par une réservation longue avec
        // "display: none". Or en CSS Grid, un élément en display:none
        // est totalement retiré du flux d'auto-placement : toutes les
        // cellules suivantes (y compris celles des AUTRES colonnes/jours)
        // se décalaient alors d'un cran, ce qui désalignait progressivement
        // tout le calendrier dès la première réservation de 2h ou plus.
        //
        // On utilise maintenant un placement EXPLICITE : chaque cellule
        // reçoit directement sa ligne et sa colonne CSS Grid calculées en
        // JS (grid-row / grid-column), indépendamment de l'ordre du DOM.
        // Les cellules couvertes par une réservation longue ne sont
        // simplement jamais créées — la cellule de la réservation, elle,
        // occupe plusieurs lignes via "grid-row: début / span N".
        // ---------------------------------------------------------------
        function renderGrid() {
            const slotsContainer = document.getElementById('calendar-slots');
            if (!slotsContainer) return;

            const days  = getWeekDays();
            const hours = getTimeSlots(); // ex: ["08","09",...,"21"]

            // Réservations visibles compte tenu des filtres de catégorie
            const visibleDateKeys = new Set(getWeekDays().map(formatDateKey));
            const filteredReservations = RESERVATIONS.filter(res => {
                const machine = MACHINES[res.machine_id];
                return machine && selectedCategories[machine.category] && visibleDateKeys.has(res.date);
            });

            // Pré-calcul : pour chaque réservation, sa durée en lignes (rowSpan),
            // sa cellule de départ (clé "jour-heure"), et l'ensemble des cellules
            // qu'elle couvre mais qui ne doivent PAS être générées séparément.
            const startMap   = new Map(); // "day-hour" -> { reservation, rowSpan }
            const coveredSet = new Set(); // "day-hour" à ne pas générer

            filteredReservations.forEach(reservation => {
                const startHour    = parseInt(reservation.start.substring(0, 2), 10);
                const startMinute  = parseInt(reservation.start.substring(3, 5), 10) || 0;
                const endHour      = parseInt(reservation.end.substring(0, 2), 10);
                const endMinute    = parseInt(reservation.end.substring(3, 5), 10) || 0;
                const durationMins = (endHour * 60 + endMinute) - (startHour * 60 + startMinute);
                const rowSpan      = Math.max(1, Math.ceil(durationMins / CALENDAR_CONFIG.minSlotDuration));

                startMap.set(`${reservation.day}-${startHour}`, { reservation, rowSpan, startHour });
                for (let i = 1; i < rowSpan; i++) {
                    coveredSet.add(`${reservation.day}-${startHour + i}`);
                }
            });

            let html = '';
            const lastHourIndex = hours.length - 1;

            hours.forEach((hourStr, hourIndex) => {
                const hourNum  = parseInt(hourStr, 10);
                const gridRow  = hourIndex + 1;
                const isLast   = hourIndex === lastHourIndex;

                // Cellule heure (colonne 1, sticky à gauche)
                html += `<div class="calendar-slot${isLast ? ' last-row' : ''}" style="grid-row:${gridRow};grid-column:1;">${hourStr}:00</div>`;

                days.forEach((_, dayIndex) => {
                    const key = `${dayIndex}-${hourNum}`;

                    // Cellule couverte par une réservation plus longue démarrée
                    // une heure plus tôt (ou plus) : on ne génère rien ici, la
                    // cellule de la réservation s'étend déjà sur cette ligne.
                    if (coveredSet.has(key)) return;

                    const gridColumn = dayIndex + 2;
                    const startInfo  = startMap.get(key);

                    if (startInfo) {
                        const { reservation, rowSpan } = startInfo;
                        const machine          = MACHINES[reservation.machine_id];
                        const status           = MACHINE_STATUS[reservation.machine_id];
                        const isMyReservation  = userId !== null && reservation.user_id === userId;

                        // La couleur du bloc indique le TYPE de réservation (cf. légende),
                        // pas la machine : 4 couleurs seulement, pour reconnaître le
                        // statut en un coup d'œil, peu importe la machine concernée.
                        //   - magenta  #9E1B56 : réservation par un autre utilisateur
                        //   - vert     #4CAF50 : ma réservation
                        //   - orange   #FF5722 : machine en maintenance
                        //   - rouge    #F44336 : machine en panne
                        // (Le champ "couleur" de MACHINES n'est donc pas utilisé ici ;
                        // il reste disponible pour d'autres usages, ex. fiches machines.)
                        let color;
                        if (status === 'maintenance')      color = '#FF5722';
                        else if (status === 'panne')       color = '#F44336';
                        else if (isMyReservation)          color = '#4CAF50';
                        else                                color = '#9E1B56';

                        let classes = 'calendar-cell reservation';
                        if (isMyReservation) classes += ' my-reservation';
                        if (isLast) classes += ' last-row';

                        html += `<div class="${classes}"
                                data-day="${dayIndex}" data-time="${hourStr}" data-machine-id="${reservation.machine_id}"
                                style="grid-row:${gridRow} / span ${rowSpan};grid-column:${gridColumn};">
                            <div class="reservation-content" style="--color:${color};">
                                <span class="reservation-machine">${machine?.nom || 'Machine'}</span>
                                <span class="reservation-user">${reservation.user}</span>
                                <span class="reservation-time">${reservation.start} – ${reservation.end}</span>
                            </div>
                        </div>`;
                    } else {
                        const lastClass = isLast ? ' last-row' : '';
                        html += `<div class="calendar-cell${lastClass}" data-day="${dayIndex}" data-time="${hourStr}"
                                style="grid-row:${gridRow};grid-column:${gridColumn};"></div>`;
                    }
                });
            });

            slotsContainer.innerHTML = html;

            // Appliquer les statuts machine PUIS les créneaux fermés
            markMachineStatus();
            markClosedSlots();
        }

        // ---------------------------------------------------------------
        // Marquage des créneaux de machines en maintenance ou en panne
        //
        // CORRECTIF : l'original marquait TOUTES les cellules vides comme
        // maintenance ou panne. On ne marque ici que les cellules qui
        // correspondent à une réservation d'une machine en statut spécial.
        // ---------------------------------------------------------------
        function markMachineStatus() {
            const slotsContainer = document.getElementById('calendar-slots');
            if (!slotsContainer) return;

            slotsContainer.querySelectorAll('.calendar-cell.reservation[data-machine-id]').forEach(cell => {
                const status = MACHINE_STATUS[parseInt(cell.dataset.machineId, 10)];
                if (!status) return;
                cell.classList.add(status); // 'maintenance' ou 'panne'
            });
        }

        // ---------------------------------------------------------------
        // Marquage des créneaux en dehors des horaires du FabLab
        // ---------------------------------------------------------------
        function markClosedSlots() {
            const slotsContainer = document.getElementById('calendar-slots');
            if (!slotsContainer) return;

            slotsContainer.querySelectorAll('.calendar-cell').forEach(cell => {
                // Ne pas écraser une réservation (qu'elle soit en maintenance/panne ou non)
                if (cell.classList.contains('reservation')) return;

                const dayIndex = parseInt(cell.dataset.day, 10);
                const hour     = parseInt(cell.dataset.time, 10);
                const fabLabHours = FABLAB_HOURS[dayIndex];

                if (fabLabHours === null) {
                    // FabLab fermé toute la journée (samedi, dimanche)
                    cell.classList.add('closed');
                } else {
                    const [openHour, closeHour] = fabLabHours;
                    if (hour < openHour || hour >= closeHour) {
                        cell.classList.add('closed');
                    }
                }
            });
        }

        // ---------------------------------------------------------------
        // Clic sur une réservation (délégation d'événement : la grille est
        // régénérée à chaque rendu, on attache donc un seul écouteur
        // permanent sur le conteneur plutôt que sur chaque cellule).
        // ---------------------------------------------------------------
        function handleCalendarClick(event) {
            const cell = event.target.closest('.calendar-cell');
            if (!cell) return;
            if (cell.classList.contains('maintenance') || cell.classList.contains('panne') || cell.classList.contains('closed')) return;

            if (!cell.classList.contains('reservation')) {
                selectFreeSlot(cell);
                return;
            }

            const dayIndex   = parseInt(cell.dataset.day, 10);
            const hourStr    = cell.dataset.time;
            const machineId  = parseInt(cell.dataset.machineId, 10);
            const visibleDateKeys = new Set(getWeekDays().map(formatDateKey));

            const reservation = RESERVATIONS.find(res =>
                res.day === dayIndex &&
                res.machine_id === machineId &&
                res.start.startsWith(hourStr) &&
                visibleDateKeys.has(res.date)
            );
            if (reservation) selectReservation(reservation, cell);
        }

        // ---------------------------------------------------------------
        // Sélection d'une réservation (affiche le bandeau d'info)
        // ---------------------------------------------------------------
        function selectReservation(reservation, cell) {
            const selectionInfo = document.getElementById('calendar-selection-info');
            const selectionText = document.getElementById('selection-info');
            const reserveBtn    = document.getElementById('reserve-btn');

            // Désélectionner la précédente
            document.querySelector('.calendar-cell.selected')?.classList.remove('selected');
            cell.classList.add('selected');
            selectedSlot = reservation;

            const machine         = MACHINES[reservation.machine_id];
            const isMyReservation = userId !== null && reservation.user_id === userId;

            if (selectionInfo && selectionText) {
                selectionText.textContent =
                    `${machine?.nom || 'Machine'} - ${reservation.start} à ${reservation.end} (${reservation.user})`;
                reserveBtn.textContent  = 'Création bientôt disponible';
                reserveBtn.disabled = true;
                selectionInfo.style.display = 'flex';
            }
        }

        function selectFreeSlot(cell) {
            document.querySelector('.calendar-cell.selected')?.classList.remove('selected');
            cell.classList.add('selected');

            const dayIndex = parseInt(cell.dataset.day, 10);
            const hourStr = cell.dataset.time;
            const startValue = buildDateTimeLocal(dayIndex, hourStr, 0);
            const endValue = buildDateTimeLocal(dayIndex, hourStr, CALENDAR_CONFIG.minSlotDuration);
            const firstMachineId = Object.keys(MACHINES)[0] || '';

            const machineInput = document.getElementById('reservation-machine');
            const startInput = document.getElementById('reservation-date-debut');
            const endInput = document.getElementById('reservation-date-fin');
            if (machineInput && !machineInput.value) machineInput.value = firstMachineId;
            if (startInput) startInput.value = startValue;
            if (endInput) endInput.value = endValue;

            const selectionInfo = document.getElementById('calendar-selection-info');
            const selectionText = document.getElementById('selection-info');
            const reserveBtn = document.getElementById('reserve-btn');
            if (selectionInfo && selectionText) {
                selectionText.textContent = `Créneau disponible le ${startValue.replace('T', ' à ')}`;
                reserveBtn.textContent = IS_AUTHENTICATED ? 'Utiliser le formulaire' : 'Connexion requise';
                reserveBtn.disabled = true;
                selectionInfo.style.display = 'flex';
            }
        }

        function initReservationForm() {
            const form = document.getElementById('reservation-form');
            if (!form) return;

            form.addEventListener('submit', async (event) => {
                event.preventDefault();
                const message = document.getElementById('reservation-message');
                const submit = document.getElementById('reservation-submit');
                const payload = {
                    machineId: form.machineId.value,
                    dateDebut: form.dateDebut.value,
                    dateFin: form.dateFin.value,
                    motif: form.motif.value
                };

                if (message) {
                    message.textContent = 'Création en cours...';
                    message.style.color = '#666';
                }
                if (submit) submit.disabled = true;

                try {
                    const response = await fetch('{{ path('api_reservation_create') }}', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json', 'Accept': 'application/json' },
                        credentials: 'same-origin',
                        body: JSON.stringify(payload)
                    });
                    const data = await response.json().catch(() => ({}));
                    if (!response.ok) {
                        throw new Error(data.error || 'Réservation refusée');
                    }
                    if (message) {
                        message.textContent = 'Réservation créée. Actualisation...';
                        message.style.color = '#2e7d32';
                    }
                    window.setTimeout(() => window.location.reload(), 700);
                } catch (error) {
                    if (message) {
                        message.textContent = error.message;
                        message.style.color = '#b00020';
                    }
                    if (submit) submit.disabled = false;
                }
            });
        }

        function buildDateTimeLocal(dayIndex, hourStr, offsetMinutes) {
            const day = getWeekDays()[dayIndex];
            const date = new Date(day);
            date.setHours(parseInt(hourStr, 10), offsetMinutes, 0, 0);
            return `${formatDateKey(date)}T${String(date.getHours()).padStart(2, '0')}:${String(date.getMinutes()).padStart(2, '0')}`;
        }

        function formatDateKey(date) {
            return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, '0')}-${String(date.getDate()).padStart(2, '0')}`;
        }

        // ---------------------------------------------------------------
        // Utilitaires date
        // ---------------------------------------------------------------

        /** Retourne le lundi de la semaine contenant `date` */
        function getWeekStartDate(date) {
            const d   = new Date(date);
            const day = d.getDay();
            d.setDate(d.getDate() - day + (day === 0 ? -6 : 1));
            return d;
        }

        /** Retourne les 7 jours de la semaine courante (lun → dim) */
        function getWeekDays() {
            const start = getWeekStartDate(currentDate);
            return Array.from({ length: 7 }, (_, i) => {
                const day = new Date(start);
                day.setDate(start.getDate() + i);
                return day;
            });
        }

        /** Retourne les heures affichées ("08", "09", …, "21") */
        function getTimeSlots() {
            return Array.from(
                { length: CALENDAR_CONFIG.endHour - CALENDAR_CONFIG.startHour },
                (_, i) => String(CALENDAR_CONFIG.startHour + i).padStart(2, '0')
            );
        }

        /** Calcule le numéro ISO de semaine */
        function getWeekNumber(date) {
            const d            = new Date(date);
            const firstDayOfYear = new Date(d.getFullYear(), 0, 1);
            const pastDays     = (d - firstDayOfYear) / 86400000;
            return Math.ceil((pastDays + firstDayOfYear.getDay() + 1) / 7);
        }

        /** Vérifie si deux dates correspondent au même jour */
        function isSameDay(d1, d2) {
            return d1.getFullYear() === d2.getFullYear() &&
                   d1.getMonth()    === d2.getMonth()    &&
                   d1.getDate()     === d2.getDate();
        }
    </script>
</body>
</html>
