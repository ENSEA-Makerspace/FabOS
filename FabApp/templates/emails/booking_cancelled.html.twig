{% extends 'emails/_layout.html.twig' %}

{% block subject %}{{ 'mail.booking.cancelled.subject'|trans({'%resource%': resource, '%booker%': booker}) }}{% endblock %}

{% block body %}
    <h2 style="margin:0 0 12px; font-size:19px;">{{ 'mail.booking.cancelled.title'|trans }}</h2>
    <p style="margin:0 0 16px;">{{ 'mail.booking.cancelled.intro'|trans({'%resource%': resource, '%booker%': booker}) }}</p>
    {% include 'emails/_booking_details.html.twig' %}
{% endblock %}
