{% extends 'emails/_layout.html.twig' %}

{% block subject %}{{ 'mail.event.cancelled.subject'|trans({'%event%': event}) }}{% endblock %}

{% block body %}
    <h2 style="margin:0 0 12px; font-size:19px; color:#b91c1c;">{{ 'mail.event.cancelled.title'|trans }}</h2>
    <p style="margin:0 0 16px;">{{ 'mail.event.cancelled.intro'|trans({'%event%': event, '%attendee%': attendee}) }}</p>
    {% include 'emails/_event_details.html.twig' %}
    <p style="margin:0; color:#6b7280; font-size:13px;">{{ 'mail.event.cancelled.note'|trans }}</p>
{% endblock %}
